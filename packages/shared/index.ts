// 权限枚举
export enum Roles {
    public = 1,
    admin = 2,
}

// 模版状态的枚举
export enum templateStatusEnum {
    DRAFT = "draft",
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
}

// 用户账号状态的枚举
export enum UserStatusEnum {
    DISABLED = 0,    // 禁用
    ACTIVE = 1,      // 正常
}

